{% extends 'base.html.twig' %}

{% block title %}{{ item.name }}{% endblock %}

{% block body %}
    <h1>{{ item.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ item.name }}</td>
            </tr>
            <tr>
                <th>Poids</th>
                <td>{{ item.weight }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary mb-2" href="{{ path('app_admin_item_index') }}">Retour à la liste</a>

    <a class="btn btn-warning mb-2" href="{{ path('app_admin_item_edit', {'id': item.id}) }}">Modifier</a>

    {{ include('admin/item/_delete_form.html.twig') }}
{% endblock %}
