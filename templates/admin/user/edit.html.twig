{% extends 'base.html.twig' %}

{% block title %}Modifier l'utilisateur {{user.firstName}}{% endblock %}

{% block body %}
    <h1>Modifier l'utilisateur {{user.firstName}}</h1>

    {{ include('admin/user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a class="btn btn-primary my-2" href="{{ path('app_admin_user_index') }}">Retour à la liste</a>

    {{ include('admin/user/_delete_form.html.twig') }}
{% endblock %}
