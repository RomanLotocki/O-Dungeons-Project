{% extends 'base.html.twig' %}

{% block title %}Modifier arme{% endblock %}

{% block body %}
    <h1>Modifier une arme</h1>

    {{ include('admin/weapon/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="btn btn-secondary mb-1" href="{{ path('app_admin_weapon_browser') }}">Retour à la liste</a>

    {{ include('admin/weapon/_delete_form.html.twig') }}
{% endblock %}
