{% extends 'base.html.twig' %}

{% block title %}Weapon{% endblock %}

{% block body %}
    <h1>Weapon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ weapon.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ weapon.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ weapon.type }}</td>
            </tr>
            <tr>
                <th>DamageDice</th>
                <td>{{ weapon.damageDice }}</td>
            </tr>
            <tr>
                <th>DamageType</th>
                <td>{{ weapon.damageType }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ weapon.weight }}</td>
            </tr>
            <tr>
                <th>Property</th>
                <td>{{ weapon.property }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_weapon_index') }}">back to list</a>

    <a href="{{ path('app_admin_weapon_edit', {'id': weapon.id}) }}">edit</a>

    {{ include('admin/weapon/_delete_form.html.twig') }}
{% endblock %}
