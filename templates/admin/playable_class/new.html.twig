{% extends 'base.html.twig' %}

{% block title %}Ajouter une classe{% endblock %}

{% block body %}
    <h1>Ajouter une classe</h1>
    {% for message in app.flashes('error') %}
        <div class="alert alert-danger mt-2" role="alert">
            {{ message }}
        </div>
    {% endfor %}

    {{ include('admin/playable_class/_form.html.twig') }}

    <a class="btn btn-primary mb-3" href="{{ path('app_admin_playable_class_index')}}">Retour à la liste</a>
{% endblock %}
