{% extends 'base.html.twig' %}

{% block title %}Modifier sort{% endblock %}

{% block body %}
    <h1>Modifier un sort</h1>

    {{ include('admin/ability/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="btn btn-secondary mb-1" href="{{ path('app_admin_ability_index') }}">Retour à la liste</a>

    {{ include('admin/ability/_delete_form.html.twig') }}
{% endblock %}
